/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.postimg.cc'], // Add the hostname here
    formats: [],
    remotePatterns: [],
  },
};

module.exports = nextConfig;
